server {
     listen [::]:80;
     listen 80;

     server_name appdruha-apps.ru www.appdruha-apps.ru;

     location ~ /.well-known/acme-challenge {
         allow all;
         root /var/www/certbot;
     }
}